# Dependency repair

Repair `work/project/calc.py` so every preserved test in `work/project/test_calc.py` passes under normal Python import and execution.

You may modify only `work/project/calc.py`. Do not modify tests, create alternate modules, or add dependencies. The verifier executes the preserved tests; it does not impose a hidden implementation grammar.
