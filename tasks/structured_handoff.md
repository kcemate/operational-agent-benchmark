# Structured handoff

Read `input/state.txt` and write `submission/handoff.json` with exactly `status`, `completed`, `next_step`, `blockers`, and `evidence_refs`.

Copy the recorded values exactly. `evidence_refs` must contain one unique entire `input/state.txt:Lx` reference for each carried fact, in source order. Do not add interpretation.
