# Log parser repair

Repair `work/project/parser.py` so every preserved test in `work/project/test_parser.py` passes under normal Python import and execution.

You may modify only `work/project/parser.py`. Write `submission/diagnosis.json` with exactly:

```json
{"defect": "<short description>", "repair_file": "work/project/parser.py"}
```

Do not modify tests or add dependencies.
